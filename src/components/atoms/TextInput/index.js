import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const TextInput = () => {
    return (
        <View>
            <Text>Text input components</Text>
        </View>
    )
}

export default TextInput

const styles = StyleSheet.create({})
